import { rangeGen } from '../../../helpers/range-gen';
import { module, test } from 'qunit';

module('Unit | Helper | range gen');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = rangeGen(42);
  assert.ok(result);
});
